{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ConstructionPressureSignal",
  "description": "Deterministic advisory signal for construction backlog pressure.",
  "type": "object",
  "required": [
    "backlog_ratio",
    "pressure_level",
    "expansion_damping",
    "rationale"
  ],
  "properties": {
    "backlog_ratio": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0
    },
    "pressure_level": {
      "type": "string",
      "enum": ["LOW", "MEDIUM", "HIGH"]
    },
    "expansion_damping": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0
    },
    "rationale": {
      "type": "string",
      "minLength": 1
    }
  },
  "additionalProperties": false
}
